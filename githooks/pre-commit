#!/usr/bin/env sh
set -u

HOOK_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
ROOT_DIR=$(CDPATH= cd -- "$HOOK_DIR/.." && pwd -P)

exec "$ROOT_DIR/plugins/agent-guard/bin/agent-guard" scan-staged
